/**
 * app-server: the runnable Quarkus deployable.
 * Contains no business logic of its own - only wiring/configuration that
 * assembles core with every adapter module (see application.properties).
 */
package com.example.hexdemo.server;
